# Configuration file for the backup scripts

numberOfDailyBackups=7

# The paths of the folders used by the scripts 
backupDirectory='/mnt/testRAID'
continuousDirectoryPath="${backupDirectory}/continuous"
dailiesDirectoryPath="${backupDirectory}/dailies"
